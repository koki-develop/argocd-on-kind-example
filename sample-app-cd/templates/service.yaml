apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-app-cd.fullname" . }}
  labels:
    {{- include "sample-app-cd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-app-cd.selectorLabels" . | nindent 4 }}
